import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
          main: '#98c447',
        },
        secondary: {
          main: '#0d1834',
        },
    },
    typography: {
        fontFamily: 'Montserrat',
    },
});
